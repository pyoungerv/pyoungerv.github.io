// Category Model
// ==============

// Includes file dependencies
define([ "jquery", "parse" ], function( $ ) {

    // The Model constructor
    var Model = Parse.Object.extend( "CategoryModel", {

    } );

    // Returns the Model class
    return Model;

} );